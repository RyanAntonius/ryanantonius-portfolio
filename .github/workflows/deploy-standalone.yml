name: Deploy standalone portfolio

on:
  push:
    branches: [main]
    paths:
      - 'independent-site/**'
      - '.github/workflows/deploy-standalone.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: independent-site/package-lock.json
      - run: npm ci
        working-directory: independent-site
      - run: npm run build
        working-directory: independent-site
      - uses: actions/upload-pages-artifact@v3
        with:
          path: independent-site/dist
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
